# 🗄️ Настройка базы данных

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Генерация Prisma Client

```bash
npm run db:generate
```

## Шаг 3: Создание таблиц в базе данных

```bash
npm run db:push
```

## Шаг 4: Заполнение базы тестовыми данными

```bash
npm run db:seed
```

## 📝 Данные для входа

После выполнения seed скрипта будут созданы следующие пользователи:

| Роль | Логин | Пароль | Имя |
|------|-------|--------|-----|
| Владелец | `owner` | `123456` | Владимир Королёв |
| РОП | `rop` | `123456` | Сергей Петров |
| Менеджер | `manager1` | `123456` | Иван Петров |
| Менеджер | `manager2` | `123456` | Мария Смирнова |
| Оператор | `operator1` | `123456` | Алексей Козлов |
| Оператор | `operator2` | `123456` | Анна Волкова |
| Оператор | `operator3` | `123456` | Дмитрий Соколов |
| Оператор | `operator4` | `123456` | Елена Морозова |

## 🎯 Тестовые данные

Seed скрипт создаст:
- ✅ 8 пользователей (1 владелец, 1 РОП, 2 менеджера, 4 оператора)
- ✅ 6 продаж
- ✅ 5 лидов
- ✅ Посещаемость за последние 7 дней для всех сотрудников
- ✅ 2 записи штрафов/бонусов
- ✅ План продаж на май 2026

## 🔧 Дополнительные команды

### Открыть Prisma Studio (GUI для базы данных)
```bash
npm run db:studio
```

### Пересоздать базу данных
```bash
npm run db:push
npm run db:seed
```

## 📊 Структура базы данных

### Таблицы:
- `users` - Пользователи системы
- `attendances` - Посещаемость
- `sales` - Продажи
- `leads` - Лиды
- `penalties_bonuses` - Штрафы и бонусы
- `sales_plans` - Планы продаж
- `achievements` - Достижения

### Связи:
- User → Attendance (один ко многим)
- User → Sale (один ко многим)
- User → Lead (один ко многим)
- User → PenaltyBonus (один ко многим)
- User → Achievement (один ко многим)
- Lead → Sale (один ко многим)

## 🚀 Запуск приложения

После настройки базы данных:

```bash
npm run dev
```

Откройте http://localhost:3000 и войдите с любым из тестовых аккаунтов!
